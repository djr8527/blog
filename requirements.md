请作为一名资深前端架构师，帮我初始化并开发一个基于 Docusaurus 的个人知识库网站。

我的核心需求是：我以后只想专注于在 docs 目录下写 Markdown 文件，通过 git push 就能自动发布到我的服务器。

具体任务如下：

1. 项目结构与导航：
   - 初始化 Docusaurus 最新版，并清理自带的 blog 示例，将首页直接设置为文档模式。
   - 配置顶部导航栏（Navbar），包含：'Java基础'、'框架与中间件'、'视频项目实战'、'面试梳理' 四个入口。
   - 配置 sidebars.js，使用 'autogenerated' 模式，让左侧菜单根据 docs 文件夹下的子目录（java, middleware, project, interview）自动生成。

2. 功能增强：
   - 集成全文搜索插件（如 docusaurus-lunr-search），确保在离线状态下也能极速检索。
   - 优化代码块显示：支持 Java, SQL, Shell, YAML 语法高亮，显示行号，并添加“一键复制”按钮。
   - 开启数学公式 (KaTeX) 支持，方便记录算法逻辑。

3. 自动化部署 (核心)：
   - 帮我编写一个 .github/workflows/deploy.yml 文件。
   - 流程要求：当我向 main 分支 push 代码时，自动运行 npm build。
   - 构建完成后，使用 ssh-deploy (或类似 Action) 通过 SSH 将 build 文件夹的内容同步到我的服务器指定目录。
   - 提示我需要在 GitHub Secrets 中配置哪些变量（如 SSH_PRIVATE_KEY, REMOTE_HOST 等）。

4. UI 与体验：
   - 风格保持极简、专业，使用系统默认字体族。
   - 支持深色模式切换。
   - 确保移动端阅读体验良好（响应式设计）。

5. 交付说明：
   - 请先帮我完成项目初始化和配置文件的修改。
   - 完成后告诉我现在应该如何创建第一篇文档来测试导航逻辑。